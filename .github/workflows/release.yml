name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: macos-latest
            target: bun-darwin-arm64
            artifact: mnem-http-mcp-darwin-arm64
          - os: macos-latest
            target: bun-darwin-x64
            artifact: mnem-http-mcp-darwin-x64
          - os: ubuntu-latest
            target: bun-linux-x64
            artifact: mnem-http-mcp-linux-x64
          - os: ubuntu-latest
            target: bun-linux-arm64
            artifact: mnem-http-mcp-linux-arm64
          - os: windows-latest
            target: bun-windows-x64
            artifact: mnem-http-mcp-windows-x64.exe

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - run: bun install

      - name: Build binary
        run: bun build --compile --target=${{ matrix.target }} src/index.ts --outfile ${{ matrix.artifact }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*
          generate_release_notes: true
